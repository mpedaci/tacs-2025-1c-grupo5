import Default from "@components/themes/themeClient/theme/default";
import {PalettesProps} from "@ant-design/colors";

const Theme = (colors: PalettesProps) => {
    return Default(colors);
}

export default Theme;